# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'KegerWeighter' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  pod 'FirebaseFirestore'
  pod 'Resolver'
  pod 'Firebase/Auth'
  pod 'Firebase/Messaging'
  pod 'Firebase/Functions'
  pod 'BottomSheet', :git => 'https://github.com/weitieda/bottom-sheet.git'
  pod 'GoogleSignIn'


  pod 'FirebaseFirestoreSwift', '8.1.0-beta'
  # Pods for KegerWeighter

  target 'KegerWeighterTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'KegerWeighterUITests' do
    # Pods for testing
  end

end
